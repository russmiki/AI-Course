# 🥗 Diet AI Chatbot (Persian)

## پروژه چت‌ بات هوشمند پیشنهاد رژیم غذایی

---

### 👤 مشخصات دانشجو

- **نام و نام خانوادگی:** مهدی ارجمند گنجی
- **رشته تحصیلی:** علوم کامپیوتر
- **عنوان پروژه:** طراحی و پیاده‌سازی چت‌بات هوشمند پیشنهاد برنامه غذایی
- **زبان پیاده‌سازی:** Python (Flask)
- **حوزه پروژه:** هوش مصنوعی / پردازش زبان طبیعی / سیستم‌های توصیه‌گر

---

## 📌 مقدمه

با توجه به افزایش اهمیت سلامت و تغذیه در زندگی روزمره، استفاده از سیستم‌های هوشمند برای پیشنهاد برنامه‌های غذایی شخصی‌سازی‌شده اهمیت ویژه‌ای پیدا کرده است.  
هدف از این پروژه، طراحی و پیاده‌سازی یک
**سیستم هوشمند پیشنهاد رژیم غذایی**
به زبان فارسی است که بتواند با دریافت اطلاعات اولیه کاربر، یک برنامه غذایی روزانه مناسب ارائه دهد

این پروژه تلاش می‌کند تا مفاهیم تئوری مطرح‌شده در دروس مرتبط با:

- برنامه‌نویسی
- هوش مصنوعی
- یادگیری ماشین
- پردازش داده

را به‌صورت عملی و کاربردی پیاده‌سازی کند.

---

## 🎯 اهداف اصلی پروژه

اهداف این پروژه به‌صورت زیر تعریف شده‌اند:

1. پیاده‌سازی یک وب‌اپلیکیشن کاربردی Flask با استفاده از
2. استفاده از دیتاست واقعی غذا برای پیشنهاد رژیم
3. طراحی منطق انتخاب غذا بر اساس وعده‌های غذایی
4. ایجاد ساختار اولیه یک چت‌بات هوشمند
5. نمایش خروجی قابل فهم و کاربردی برای کاربر نهایی

---

## 🧠 منطق عملکرد سیستم

منطق کلی سیستم به‌صورت مرحله‌به‌مرحله به شرح زیر است:

1. کاربر وارد صفحه اصلی وب‌اپلیکیشن می‌شود
2. اطلاعات اولیه شامل قد و وزن را وارد می‌کند
3. سیستم اطلاعات واردشده را دریافت و اعتبارسنجی می‌کند
4. (CSV) دیتاست غذاها از فایل خوانده می‌شود
5. غذاها بر اساس نوع وعده غذایی دسته‌بندی می‌شوند
6. برای هر وعده، تعدادی غذا به‌صورت تصادفی انتخاب می‌شود
7. برنامه غذایی نهایی به کاربر نمایش داده می‌شود

این روند باعث می‌شود سیستم به‌صورت ساده اما کاربردی عمل کند.

---

## 📸 مستندات اجرای پروژه (photos)

### (photo 1): صفحه اصلی برنامه

در این تصویر، صفحه اصلی وب‌اپلیکیشن نمایش داده شده است که شامل فرم ورود اطلاعات کاربر می‌باشد.

![alt text](<photo/photo 1.png>)

---

### 🔹(photo 2): وارد کردن اطلاعات کاربر

در این مرحله، کاربر قد و وزن خود را در فرم وارد کرده و درخواست دریافت برنامه غذایی می‌دهد.

![alt text](<photo/photo 2.png>)

---

### 🔹 (photo 3): نمایش برنامه غذایی پیشنهادی

پس از پردازش اطلاعات، برنامه غذایی روزانه شامل وعده‌های مختلف به کاربر نمایش داده می‌شود.

![alt text](<photo/photo 3.png>)

---

## 📁 ساختار پروژه

ساختار فایل‌ها و پوشه‌های پروژه به شکل زیر طراحی شده است:

```
diet-ai-chatbot-fa/
│
├── app.py
├── requirements.txt
├── train_intent_model.py
│
├── data/
│   ├── foods.csv
│   └── intents.csv
│
├── models/
│   ├── intent_model.pkl
│   └── vectorizer.pkl
│
├── chatbot/
│   ├── agent.py
│   └── memory.py
│
├── nlp/
│   └── preprocess.py
│
├── recommender/
│   ├── rules.py
│   └── explain.py
│
├── templates/
│   └── index.html
│
└── static/
    └── style.css
```

---

## 📄 توضیح بخش‌های اصلی پروژه

### 🔹 فایل app.py

این فایل به‌عنوان هسته اصلی برنامه عمل می‌کند و شامل:

- راه‌اندازی سرور Flask
- مدیریت درخواست‌های کاربر
- دریافت داده‌ها از فرم
- پردازش دیتاست غذا
- ارسال نتایج به رابط کاربری

می‌باشد.

---

### 🔹 دیتاست foods.csv

این فایل شامل اطلاعات غذاهای مختلف بوده و هر سطر نشان‌دهنده یک غذای مجزا است.  
داده‌های این فایل مبنای اصلی سیستم پیشنهاد غذا را تشکیل می‌دهند.

---

### 🔹 بخش NLP

در این پروژه، از مفاهیم اولیه پردازش زبان طبیعی برای تشخیص نیت کاربر استفاده شده است.  
این بخش به‌عنوان زیرساخت توسعه‌های آینده در نظر گرفته شده است.

---

### 🔹 سیستم توصیه‌گر (Recommender)

منطق پیشنهاد غذا در این پروژه مبتنی بر:

- نوع وعده غذایی
- انتخاب تصادفی کنترل‌شده
- قوانین ساده تغذیه‌ای

می‌باشد که پایه‌ای مناسب برای توسعه سیستم‌های هوشمندتر است.

---

## 🚀 نحوه اجرای پروژه

برای اجرای پروژه مراحل زیر انجام می‌شود:

```bash
pip install -r requirements.txt
python app.py
```

سپس از طریق مرورگر، آدرس زیر باز می‌شود:

```
http://127.0.0.1:5000
```

---

## 🛠 ابزارها و تکنولوژی‌های استفاده‌شده

- Python
- Flask
- Pandas
- Scikit-learn
- HTML / CSS

---

## ✅ جمع‌بندی نهایی

این پروژه با هدف پیاده‌سازی عملی مفاهیم هوش مصنوعی و برنامه‌نویسی طراحی شده و به‌صورت کامل اجرا و تست شده است.  
خروجی‌های پروژه در قالب اسکرین‌شات‌های ارائه‌شده قابل مشاهده بوده و ساختار پروژه قابلیت توسعه و بهبود در آینده را دارا می‌باشد.
